const CreateCollection = () => {
    // one form
    // fields:
        // name
        // fields
    // all fields combined make up the template
    // the fields section should be made of the following components:
        // key
        // display title
        // add field button
    // have a limit of 5 fields for now
    // save by making a call to the create collection endpoint
}

export default CreateCollection;
